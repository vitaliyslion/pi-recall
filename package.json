{
  "name": "pi-recall",
  "version": "0.1.0",
  "private": true,
  "description": "Pi extension: capture full bash output, index it (Orama BM25), and show the model a compact recall stub — reversible compaction.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "check": "node --check src/index.js && node --check src/store.js && node --check src/stub.js && node --check src/config.js",
    "smoke": "node test/smoke.js",
    "format": "oxfmt",
    "format:check": "oxfmt --check"
  },
  "dependencies": {
    "@orama/orama": "^3.1.18",
    "@orama/plugin-data-persistence": "^3.1.18",
    "typebox": "^1.1.38"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.4",
    "oxfmt": "^0.55.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.79.0"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "pi": {
    "extensions": [
      "./index.js"
    ]
  }
}
